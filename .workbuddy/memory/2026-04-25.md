# 2026-04-25 工作日志

## Forest Family 项目本地运行 ✅

用户提供了 GitHub 仓库 `dinghao7-del/-` 的 Personal Access Token，成功完成以下操作：

### 仓库信息
- 仓库地址：`https://github.com/dinghao7-del/-`
- 克隆目录：`/tmp/forest-family`
- 技术栈：**Vite + React 19 + TypeScript + TailwindCSS v4**

### 关键技术细节
- 路由：React Router v7，共 18 个页面路由（比设计稿多了 habits/history/welcome 等）
- AI：集成 `@google/genai`（Gemini API），需要 `GEMINI_API_KEY`
- 动画：`motion` 库（Framer Motion 继任）
- 状态管理：React Context（FamilyContext）

### 运行步骤（踩坑记录）
1. npm 安装需要配置 socks5h 代理：`npm config set proxy socks5h://127.0.0.1:1080`
2. 同时需要关闭 strict-ssl：`npm config set strict-ssl false`
3. 注意：国内 npmmirror 源连接不稳定，用官方源 + socks5h 代理更可靠
4. 启动端口：3000，命令 `npm run dev`

### 当前状态
- 服务运行于 `http://localhost:3000`（PID 31338）
- `.env` 里的 `GEMINI_API_KEY` 为占位符，AI 功能暂未配置

### 代码审核 & 修复（2026-04-25）
审核了全部 30 个源文件，发现并修复了以下问题：
- ✅ `redeemReward` 未扣减星星余额（Bug）
- ✅ `alert()` 替换为 Toast 通知（新建 ToastContainer 组件）
- ✅ 硬编码真实姓名（坦坦/然然/妈妈等）替换为「家长 A/B/C」「孩子 A/B」
- ✅ CSS 主题色 #006e1c → #2E7D32（与设计稿一致）
- ✅ 历史记录（history）添加 localStorage 持久化

待处理：习惯与任务逻辑对齐（习惯类型 type='表扬'/'penalty'/'学习' 的定义逻辑待确认）
